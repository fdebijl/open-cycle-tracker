# Copy to .env to override. The default already targets the local API.

# Base URL of the Open Cycle Tracker API.
VITE_API_URL=http://localhost:3000

# Demo mode is configured at RUNTIME, not here: set PUBLIC_DEMO_MODE=true on the
# UI container (it's written into /config.js). See ui.dockerfile / compose.yaml.
